require "spec_helper"

describe ReplaceRegex do
  it "has a version number" do
    expect(ReplaceRegex::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
